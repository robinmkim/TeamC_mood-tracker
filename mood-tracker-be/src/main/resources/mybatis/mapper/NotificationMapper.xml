<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamc.moodtracker.dao.NotificationDao">

    <select id="selectMyNotificationAll" parameterType="int" resultType="NotificationDto">
        SELECT * FROM notice
        WHERE n_user = #{n_user}
        ORDER BY n_id DESC
    </select>

    <select id="selectMyNotificationFollow" parameterType="int" resultType="NotificationDto">
        SELECT * FROM notice
        WHERE n_user = #{n_user} AND n_type = 'follow'
        ORDER BY n_id DESC
    </select>

    <select id="selectMyNotificationComment" parameterType="int" resultType="NotificationDto">
        SELECT * FROM notice
        WHERE n_user = #{n_user} AND n_type = 'comment'
        ORDER BY n_id DESC
    </select>

    <select id="selectMyNotificationLike" parameterType="int" resultType="NotificationDto">
        SELECT * FROM notice
        WHERE n_user = #{n_user} AND n_type = 'like'
        ORDER BY n_id DESC
    </select>

    <select id="selectUnreadNumber" parameterType="int" resultType="int">
        SELECT count(*) FROM notice
        WHERE n_state = #{n_user}
    </select>



</mapper>